## 1、For experimental environment requirements, please refer to requirement.txt.
The code for model training is in the train.py file; the model implementation code is stored in the model folder; the trained models are saved in the model_save folder.
## 2、 Baseline Models
Perturbation-based models: Vanilla GNNExplainer, PGExplainer, SubgraphXSurrogate models: PGM-Explainer, DnX
## 3、 Experimental Results
The explanation results are stored in the explain_results folder.
## 4、 Others
The code for the explanation model is in the explain.py file; the explainer implementation code is stored in the explainer folder; the trained PGExplainer and DnX models are saved in the explain_model_save folder.
